services:
  postgres:
    image: postgres:16
    container_name: msi_strapi_db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: msi-admin
      POSTGRES_PASSWORD: qwertikov123
      POSTGRES_DB: msi-db
    volumes:
      - ./postgres-data:/var/lib/postgresql/data